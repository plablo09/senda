events {}
http {
    # Use Docker's embedded DNS so upstream hostnames are resolved at request time
    resolver 127.0.0.11 valid=10s;

    server {
        listen 80;

        # WebSocket support
        location /ws/ {
            set $api_upstream http://api:8000;
            proxy_pass $api_upstream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_read_timeout 3600s;
        }

        # Strip /api prefix before forwarding to FastAPI
        location /api/ {
            set $api_upstream http://api:8000;
            client_max_body_size 52m;
            rewrite ^/api/(.*) /$1 break;
            proxy_pass $api_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # MinIO public artifact proxy — resolves todo 008
        location /artefactos/ {
            set $minio_upstream http://minio:9000;
            rewrite ^/artefactos/(.*) /senda-documentos/$1 break;
            proxy_pass $minio_upstream;
            proxy_set_header Host minio;
        }

        location / {
            set $frontend_upstream http://frontend:5173;
            proxy_pass $frontend_upstream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
        }
    }
}
